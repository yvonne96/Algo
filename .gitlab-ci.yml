image: python

before_script:
  - apt update
  - pip3 install -r requirements.txt
  - cd code
  - npm install
  - cd

test:
  script:
    - python3 -m unittest discover -v
    - cd code
    - npm test