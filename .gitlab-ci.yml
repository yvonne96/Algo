test:algorithm:
    image: python
    script:
        - apt update
        - pip3 install -r requirements.txt
        - python3 -m unittest discover -v
    
test:main:   
    image: node:latest
    cache:
      paths:
        - node_modules/
    script:
        - cd code
        - npm install
        - npm test
        